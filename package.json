{
  "name": "musilink-firestore-rules",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --project musilink-rules-test --only firestore \"node --test tests/firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}

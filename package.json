{
  "name": "musilink-firestore-rules",
  "private": true,
  "scripts": {
    "test:web-shell": "node --test tests/pwa_viewport.test.cjs",
    "test:rules": "firebase emulators:exec --project musilink-rules-test --only firestore \"node --test tests/firestore.rules.test.cjs\"",
    "test:functions:usernames": "npm --prefix functions run build && firebase emulators:exec --project musilink-functions-test --only firestore \"node --test functions/test/username_claim.test.cjs\"",
    "test:functions:social": "npm --prefix functions run test:social",
    "test:functions:chat": "npm --prefix functions run build && firebase emulators:exec --project musilink-functions-test --only firestore \"node --test functions/test/chat_cleanup.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}

{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test:account-deletion": "npm run build && node --test test/account_deletion.test.cjs",
    "test:social": "npm run build && firebase emulators:exec --config ../firebase.json --project musilink-functions-test --only firestore \"node --test test/social_writes.test.cjs\"",
    "test:music-profile": "npm run build && firebase emulators:exec --config ../firebase.json --project musilink-functions-test --only firestore \"node --test test/music_profile_write.test.cjs\"",
    "test:unit": "npm run build && node --test test/unit/*.test.cjs",
    "sanitize:music-profiles": "node scripts/sanitize_music_profiles.cjs"
  },
  "engines": {
    "node": "24"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "private": true
}
